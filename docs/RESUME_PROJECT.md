# 简历项目描述

## AI 作业报告生成与质量监控系统

后端开发 / Agent 应用

### 项目简介

面向课程作业、论文阅读、实验报告和项目规划等场景，设计并实现一套基于 RAG 与 Agent 工作流的智能报告生成系统。系统支持资料解析、向量索引、任务类型识别、证据检索、报告生成、质量门控、自动改写、报告编辑导出与监控看板，将大模型直接生成不可控的问题拆解为可观测、可回溯、可优化的生成闭环。

### 技术栈

Spring Boot 3、Java 21、MyBatis Plus、MySQL、SSE、FastAPI、ChromaDB、DeepSeek、DashScope Embedding、Docker Compose

### 核心功能

资料上传与解析、RAG 检索、任务类型识别、Agent 报告生成、质量门控、自动改写、报告编辑导出、数据监控

### 主要工作

- 设计并实现 Agent 生成工作流，将一次报告生成拆分为资料索引、任务类型识别、RAG 检索、报告生成、质量检查、自动改写、结果落库等阶段，并通过 Trace 记录每阶段耗时、检索片段数、质量评分和改写采纳结果，提升生成过程的可解释性。

- 搭建基于 ChromaDB 的 RAG 检索链路，按作业维度隔离并重建向量索引，避免历史资料残留导致 RAG 召回污染；设计“作业信息 Query、Skill Query、章节 Query”三路确定性检索扩展，按 `chunk_id` 去重后取 Top-K 片段入上下文，测试样本中 RAG 检索平均耗时约 358ms。

- 设计任务类型识别机制，结合规则路由与 LLM 路由识别论文总结、实验报告、项目规划等生成策略，并将路由原因、置信度和匹配结果以中文日志展示，降低 Agent 黑盒感，便于用户理解系统为何选择当前生成流程。

- 实现质量门控与自动改写机制，以模型质量评分为主、本地规则兜底，围绕结构完整性、证据引用、表达具体性、占位符残留等维度判断报告是否可提交；当报告低于阈值时触发一次最小必要改写，并在改写后重新评分，仅采纳不低于初稿质量的版本，避免“越改越差”。

- 构建报告生成监控看板，统计任务完成率、质量通过率、自动改写触发率、改写采纳率、平均耗时、P95 耗时、平均检索片段数和任务类型分布等指标；测试样本中端到端平均生成耗时约 79s，P95 耗时约 132s，可定位主要耗时集中在模型生成、质量检查与自动改写阶段。

